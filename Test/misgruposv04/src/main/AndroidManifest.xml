<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="py.com.misgruposv04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MenuPrincipalActivity" />
        <activity android:name=".activities.AcercaDeActivity" />
        <activity android:name=".activities.CrearGrupoActivity" />
        <activity android:name=".activities.CrearCuentaActivity" />
        <activity android:name=".activities.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
